# Tests

Use this directory for regression tests and evaluation fixtures.

Recommended first tests:

- Trace objects can serialize to JSON.
- Rubric scores stay within expected ranges.
- Reflection memory does not overwrite older memory versions.
- Baseline and memory-enabled workflows can be compared on the same task set.
